# GitHub Sponsors / funding options for LiteMLflow
# Activate each before launch — placeholder entries are ignored by GitHub until the account is registered.

github: [dolonet]           # TODO: activate GitHub Sponsors at github.com/sponsors/dolonet
custom:
  - https://polar.sh/litemlflow   # TODO: create Polar.sh org before launch
